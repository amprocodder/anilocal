package com.anilocal.app.extensions

/**
 * Placeholder so the :extensions source set compiles before the vendored Aniyomi source-api and
 * [AniyomiSourceAdapter] land (Phase 2, sub-steps 2c–2g). Replaced by real code; exists now only
 * to give CI a non-empty module to build at the 2a checkpoint.
 */
internal object ExtensionsModule
