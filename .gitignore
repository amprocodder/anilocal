# Build outputs
build/
captures/
.externalNativeBuild/
.cxx/

# Gradle
.gradle/
local.properties

# IDE
*.iml
.idea/
.DS_Store

# On-device dev/test chain — local-only, not committed (see devenv/scripts/README.md)
devenv/

# Kotlin compiler session metadata
.kotlin/

# Secrets / signing (never commit these)
*.keystore
*.jks
keystore.properties
# …except the DEBUG keystore: deliberately committed (standard non-secret debug key) so local
# and CI debug APKs share one signing identity and can update-install over each other.
!app/debug.keystore
